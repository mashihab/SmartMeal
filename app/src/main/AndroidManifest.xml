<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartmeal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.ForgetPass"></activity>
        <activity android:name=".ui.AllMemberProfile" />
        <activity android:name=".ui.AddMemberDeposit" />
        <activity android:name=".ui.AddDailyCost" />
        <activity android:name=".ui.loginsignup.Login"></activity>
        <activity android:name=".ui.loginsignup.SignUp" />
        <activity android:name=".ui.UserActivity"></activity>
        <activity android:name=".ui.AddMeal" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>